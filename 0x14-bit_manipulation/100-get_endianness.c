#include "main.h"

/**
 * get_endianness -  checks the endianness
 *
 * Return: if endian 1 otherwise 0.
 */
int get_endianness(void)
{
	unsigned int num = 1;
	char *end = (char *)&test;

	if (*endian)
		return (1);

	return (0);
}
